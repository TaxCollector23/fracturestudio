export function ApiDocsPage() {
  return <main className="px-6 py-14 sm:px-10"><section className="mx-auto max-w-5xl"><p className="text-xs uppercase tracking-[0.45em] text-zinc-500">API</p><h1 className="mt-4 font-serif text-5xl italic sm:text-6xl">Fracture API.</h1><p className="mt-5 text-sm leading-7 text-zinc-400">POST /api/chat with action fracture, chat, citations, or rebuttals. The response is OpenRouter-shaped and the UI gracefully falls back to local engines.</p><pre className="mt-8 overflow-auto border border-zinc-900 bg-[#0c0c0e] p-5 text-sm text-zinc-300">{`POST /api/chat\n{ "action": "chat", "messages": [{ "role": "user", "content": "help" }] }`}</pre></section></main>;
}
