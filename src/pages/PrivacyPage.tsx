export function PrivacyPage() {
  return <main className="px-6 py-14 sm:px-10"><section className="mx-auto max-w-5xl"><p className="text-xs uppercase tracking-[0.45em] text-zinc-500">Privacy</p><h1 className="mt-4 font-serif text-5xl italic sm:text-6xl">Clear data handling.</h1><p className="mt-5 text-sm leading-7 text-zinc-400">Drafts stay in the browser until the user sends an AI request. Auth uses Supabase when configured; otherwise the app remains in local fallback mode.</p></section></main>;
}
