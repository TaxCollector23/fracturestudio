{
  "name": "fracture-studio",
  "version": "1.0.0",
  "description": "Fracture Studio — AI-powered argument, speech, and essay analysis engine (v1 rework)",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "firebase-admin": "^13.10.0",
    "pdfkit": "^0.17.2"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
